{
  "name": "typeinside",
  "description": "promise-based dcinside unofficial api(wip) for personal use",
  "devDependencies": {
    "@types/node": "^12.12.14",
    "chai": "^4.2.0",
    "coveralls": "^3.0.9",
    "istanbul": "^0.4.5",
    "mocha": "^6.2.2",
    "typescript": "^3.7.3"
  },
  "scripts": {
    "start": "tsc && node ./dist/",
    "build": "tsc",
    "test": "tsc && mocha --timeout 30000 --reporter spec",
    "cover": "tsc && istanbul cover node_modules/mocha/bin/_mocha test/*.js -- --timeout 30000 -R spec"
  },
  "license": "MIT",
  "author": "marscop <marscop7@gmail.com>",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "version": "0.0.10",
  "repository": {
    "type": "git",
    "url": "https://github.com/Akachu/typeinside.git"
  }
}
